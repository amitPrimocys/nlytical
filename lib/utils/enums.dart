enum DropDownType { category, subCategory }

enum UserType { isUser, isVendor }

enum UserRole { user, vendor }

enum FilterClear {
  categoryClear,
  sunCatClear,
  priceClear,
  ratingClear,
  locationClear,
}
